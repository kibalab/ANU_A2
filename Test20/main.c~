#include <stdio.h>
int main()
{
	int i = 0;
	scanf("%d", &i);

	for(int h = 0; h < i; h++){
		for(int k = i - h; k > 0; k--)
		{
			printf(" ");
		}
		for(int l = 0; l < h; l++)
		{
			printf("*");
		}
		printf("\n");
	}
	return 0;
}
