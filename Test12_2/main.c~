#include <stdio.h>

int main()
{
	int a = 1;

	while(a >= 1 && a <= 10)
	{
		scanf("%d", &a);
		
		while(a > 0)
		{
			printf("\nPrint %d" ,a--);
		}
		
		a = 1;
		printf("\n");
	}

	printf("\nGoodbye");
	return 0;
}
